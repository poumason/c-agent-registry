# 0006 — ERD 圖同步角色改版後的欄位命名

**Commit**: `5661116` · 2026-08-05

## 做了什麼

使用者自己更新了 `idea.drawio`，把 ERD 圖上的 `User_Agent_Rel.role` 欄位標籤與 `Asset_Role`
swimlane 內容（`Admin` → `Owner` 等）同步成 0003 角色改版後的實際命名，讓圖與程式碼一致。
